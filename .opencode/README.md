# OpenCode

The root `opencode.json` starts the same local STDIO MCP server used by other
Agents. The business workflow remains in the shared
`.agents/skills/llm-wiki-builder/SKILL.md`; do not create an OpenCode-specific
copy.
